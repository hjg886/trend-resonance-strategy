# -*- coding: utf-8 -*-
"""① v4.7-R10 主文档第二章补「规则变更边界」第7条；② 安盈六策旧原则改写为修正版。"""
import io, os, shutil, sys

MAIN = r"E:\fnOS\文档\证券\中线趋势共振策略\中线趋势共振策略 v4.7-R10 回测驱动重构版（1+2架构）完整补齐版.txt"
OLD = r"E:\fnOS\文档\证券\安盈六策 以前策略\只增不减 核心规则永久锁定原则.txt"

ok = True

# ---- ① 主文档插入 ----
old_main = "6. 【重构 R-04 补充哲学·环境大于信号】：回测归因证明，同一信号的期望收益在不同波动率环境下差异极大（S1+低波动期望-4.68% vs S2+中波动+1.67%）——**市场环境是信号的第一过滤器**，环境不配合时再强的信号也应放弃。\n\n---"
new_main = ("6. 【重构 R-04 补充哲学·环境大于信号】：回测归因证明，同一信号的期望收益在不同波动率环境下差异极大（S1+低波动期望-4.68% vs S2+中波动+1.67%）——**市场环境是信号的第一过滤器**，环境不配合时再强的信号也应放弃。\n"
            "7. 【规则变更边界】（2026-08-21 修正，替代「只增不减、核心永久锁定」）：本章投资哲学为价值观，永久固化；但**规则变更**遵循「分层冻结 + 证据驱动变更」——①Bug修复：随时修（记录+回归测试）；②参数校准：样本外校准（IS+OOS 回测）；③结构变更：冻结（全周期回测+OOS+压力测试）；④策略失效：14A 健康度仪表盘触发时允许重新设计。禁止因单笔/短期实盘亏损改参数（防情绪化）、禁止样本内反复调参（防过拟合）；已固化裁决（R-01~R-10、F-01~F-35、P12v6、P0-P12/E-P0.0~E-P12）非有回测证据不得改动。详见配套《规则变更边界原则（v4.7-R10 修正版）》。\n\n---")

with io.open(MAIN, "r", encoding="utf-8") as f:
    c = f.read()
n = c.count(old_main)
if n != 1:
    print(f"[FAIL] 主文档 count={n}")
    ok = False
else:
    c = c.replace(old_main, new_main, 1)
    with io.open(MAIN, "w", encoding="utf-8", newline="") as f:
        f.write(c)
    print("[OK] 主文档第二章已补第7条「规则变更边界」")

# ---- ② 安盈六策旧文件改写 ----
if not os.path.exists(OLD + ".bak_20260821"):
    shutil.copy2(OLD, OLD + ".bak_20260821")
    print(f"[BAK] {OLD}.bak_20260821")

new_content = """# 安盈六策 规则变更边界原则（修正版）
# （2026-08-21 起替代「只增不减、核心永久锁定」）

**核心原则**：分层冻结、证据驱动变更。先判变更类型，再定边界；非 Bug 类变更须经 2020-2026 全周期回测 + 样本外验证。
**硬性底线**：最大回撤 ≤ 12%。（删去原「胜率≥80%」——中线趋势策略正常胜率 40-60%，80% 门槛会逼策略为胜率牺牲收益。）

---

## 〇、变更四分类（先判类型，再定边界）
| 变更类型 | 边界 | 前置条件 |
|----------|------|----------|
| Bug 修复 | 随时修 | 记录 + 回归测试 |
| 参数校准 | 季度/年度窗口 | 回测验证 |
| 结构新增 | 只增不减 | 回测验证 |
| 结构冻结 | 永久锁定 | — |
| 策略失效 | 允许重新设计 | 健康度仪表盘触发 |

---

## 一、✅ Bug 修复（随时修，新增）
- 定义：规则实现与文档不一致的执行错误。
- 边界：随时修，只需书面记录 + 回归测试（逐笔一致断言）。
- 举例：ETF 止损公式 P1→E-P1、六灯第1/6灯重复判定合并。

---

## 二、✅ 季度复盘可微调（参数校准，每季度1次）
| 参数类别 | 允许微调范围 | 验证要求 |
|----------|--------------|----------|
| MACD分级阈值 | 标准/稳健/防守级参数±2 | 信号准确率波动<5% |
| 止盈调整系数 | 爆发期上浮1.3-1.7倍；移动止损2%-4% | 单笔盈利提升<15%，回撤不增加 |
| ATR波动率分界 | 高/中/低波动±0.5%（默认1.5%/3%） | 止盈误触发率波动<5% |
| 行业动量衰减 | 半衰期50-70日；每10日扣0.05-0.15分 | 行业轮动胜率≥85% |
| 基本面质量分 | 净利润/ROE/现金流权重±0.1 | 暴雷率<1% |

---

## 三、✅ 年度复盘可调整（参数校准，需3年牛熊回测）
1. XGBoost 18个核心因子权重（**不得增删因子**）
2. 多因子替代得分权重（±0.05，默认0.4/0.3/0.3）
3. 行业四维打分权重（±0.1，默认各0.25）
4. 个股共振得分权重（±0.2，前高后低逻辑不变）

---

## 四、✅ 允许永久补充（结构新增，只增不减，不覆盖原有规则）
1. 新增持仓个股的专属终极止损价
2. 补充极端场景（熔断/停牌/退市）处理规则
3. 增加量化模型辅助工具
4. 完善复盘统计指标
5. 补充实盘日志与错误案例

---

## 五、❌ 绝对禁止修改（结构冻结，永久锁定）
### 核心原则
- 月度调仓、月中只卖不买
- 永远≥30%现金、单票≤12%、单行业≤40%
- 六大禁令：不补仓/不摊薄/不做T/不追高/不加仓/不扛单

### 规则框架
- 大盘→行业→个股三层选股逻辑
- 卖出风控优先级（终极止损>行业破线>浮亏止损>均线>MACD>回撤>月末）
- 双MACD+量能+均线三重技术过滤

### 固定铁律
- 浮亏-6%/-10%无条件清仓
- 沪深300单周跌>5%暂缓止损3天
- XGB>60、综合预判≥70买入门槛
- 原有个股终极止损：三一17.00、阳光103、长信6.40、广核4.10/4.20

---

## 六、策略失效出口（新增）
- 策略健康度仪表盘触发「策略可能失效」标记（超额收益滚动Z-score<−2 / 信号命中率低于回测15pp / 胜率偏离20pp）→ 全面复核（正常波动/参数失配/策略失效三档）→ 判定「策略失效」时允许重新设计。

---

## 修改标准流程
提出假设 → 全周期回测 → 压力测试 → 样本外验证 → 书面记录 → 模拟盘1个月 → 正式生效

**最终底线**：任何可能导致最大回撤>12% 的调整，无论收益多高，一律放弃。
"""

with io.open(OLD, "w", encoding="utf-8", newline="") as f:
    f.write(new_content)
print("[OK] 安盈六策旧原则已改写为修正版")

print("\nALL DONE" if ok else "\nSOME FAILED")
sys.exit(0 if ok else 1)
